export type {
  Project,
  ProjectEnvironment,
  TerminalEntry,
  LocalConnectionContext,
  WslConnectionContext,
  RemoteConnectionContext,
  ConnectionContext,
  ProjectView,
  ProjectCommands,
  ProjectCapabilities,
  ActiveProjectContext,
} from '@/shared/types/project';

export { environmentToConnectionContext } from '@/shared/types/project';
