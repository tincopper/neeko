// Re-export from shared for backward compatibility
export {
  ConnectionProjectProvider,
  useConnectionProjectContext,
} from '@/shared/contexts/ConnectionProjectContext';
export type {
  WslContextValue,
  RemoteContextValue,
  ConnectionProjectContextValue,
} from '@/shared/contexts/ConnectionProjectContext';
